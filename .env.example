# Comma-separated list of origins allowed to connect to the socket.io server.
# Leave unset for dev (defaults to '*').
# Example for production: SOCKET_IO_CORS_ORIGIN=https://chat.example.com,https://www.example.com
SOCKET_IO_CORS_ORIGIN=

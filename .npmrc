# Quieter, leaner install for memory-constrained CI hosts (Render free tier
# is 512 MB and `npm ci` was being killed mid-install).
audit=false
fund=false
loglevel=error
progress=false
prefer-offline=true
